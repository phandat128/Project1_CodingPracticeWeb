generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model problem {
  id            Int    @id @default(autoincrement())
  name          String
  topic         String
  input         String
  output        String
  exampleInput  String
  exampleOutput String
}
